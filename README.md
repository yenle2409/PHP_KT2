# PHP_KT1
index.php              → Form upload + hiển thị gallery (Front-end)
├── upload.php         → Xử lý upload, kiểm tra, nén, resize
├── db.php             → Kết nối CSDL MySQL
├── compress.php       → Hàm nén ảnh
├── resize.php         → Hàm resize ảnh
├── uploads/           → Thư mục lưu ảnh đã upload
└── style.css          → Giao diện CSS đơn giản